// Simplex example: Factorial
// Run with: node bin/simplex.js examples/factorial.sim

fn factorial(n: Int) -> Int {
  if n <= 1 {
    return 1;
  }
  n * factorial(n - 1)
}

let n: Int = 10;
let result: Int = factorial(n);
print("factorial(" + to_string(n) + ") = " + to_string(result))
