// Mere example: Factorial
// Run with: node bin/mere.js examples/factorial.sim

fn factorial(n: Number) -> Number {
  if n <= 1 {
    return 1;
  }
  n * factorial(n - 1)
}

let n: Number = 10;
let result: Number = factorial(n);
print("factorial(" + to_string(n) + ") = " + to_string(result))
